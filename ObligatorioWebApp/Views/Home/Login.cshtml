@model LogicaAplicacion.DTOs.UsuarioDTO

@{
    ViewData["Title"] = "Login";
}

<h1>Login</h1>


@if (!string.IsNullOrWhiteSpace(ViewBag.Error))
{
    if (ViewBag.Error.Contains("iniciar sesión"))
    {
        <div class="alert alert-warning">
            @ViewBag.Error
        </div>
    } else
    {
        <div class="alert alert-danger">
            @ViewBag.Error
        </div>
    }
    
}

<div class="row">
    <div class="col-md-4">
        <form asp-action="Login">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label for="Email" class="control-label">Email</label>
                <input id="Email" name="Email" class="form-control" required />
            </div>

            <div class="form-group">
                <label for="password" class="control-label">Clave</label>
                <input id="password" name="password" type="password" class="form-control" required/>
            </div>
            <br/>
            <div class="form-group">
                <input type="submit" value="Login" class="btn btn-primary rounded-pill shadow-sm" />
            </div>
        </form>
    </div>
</div>
